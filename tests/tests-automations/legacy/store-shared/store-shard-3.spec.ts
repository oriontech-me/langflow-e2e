import { test } from "@playwright/test";

// Langflow Store was discontinued. Tests preserved for when the feature returns.
// Previously required STORE_API_KEY to run.

test.skip("should order the visualization (requires store API key)", () => {});
test.skip("should filter by type (requires store API key)", () => {});
