{
  "_comment": "USD per 1M tokens. Hand-edited in a PR. A model absent here keeps its token counts and reports usd_estimated: null — see scripts/lib/token-cost.mjs. Sourced 2026-07-31 (#1197 review, finding I6; gemini-3.5-flash added #1197 re-review, finding B, from run 30647253368 — the run's largest consumer, 13 calls / 14,690 tokens, previously unpriced; the three gemini-*-lite rows added #1211 review round 2) from Anthropic's published pricing (platform.claude.com/docs/en/pricing) and Google's public Gemini pricing page — re-verify before trusting a number more than a few months old. A model id absent EXACTLY is also matched by substring against every key here (both directions — a dated/preview/latest id contains its family key, a short alias is contained by a longer one), longest key wins when more than one matches, gated on the leftover being recognizable version/alias noise (a date, `-latest`, `-preview`, `-search-preview`) rather than a different product's name — see token-cost.mjs's isAllowedSuffix() — so a rotated `-preview`/`-latest`/dated id does not need its own row unless its rate actually differs from its family's, but a TIER id (Lite, Mini, Nano, Pro, ...) always needs its own row: the substring pass deliberately refuses to guess a tier's price from its non-tier sibling. A model's value is either a flat rate ({inputPerMillion, outputPerMillion} — the common case) or a dated-bands array ([{since, inputPerMillion, outputPerMillion}, ...], see claude-sonnet-5 below) when the rate has actually changed over time; `--summarize` selects the band effective on the run's own date (RUN_DATE), never the newest by default. gemini-flash-latest and gemini-3.5-flash are priced the same as gemini-2.5-flash — the Flash tier's pricing has stayed stable across recent dot-releases and gemini-flash-latest resolves to this exact class on Langflow 1.12.0.dev10 (measured: the trace's modelName is the bare alias, not a dated id) — re-check these two rows if either moves to pricing distinct from 2.5-flash. Same reasoning extends to the Lite tier: gemini-2.5-flash-lite, gemini-3.5-flash-lite and gemini-flash-lite-latest are all priced at Google's published Gemini 2.5 Flash-Lite rate ($0.10/$0.40 per 1M, ai.google.dev/gemini-api/docs/pricing, sourced 2026-07-31) — re-check if 3.5's Lite tier is ever priced separately from 2.5's. EVERY entry (and every band of a dated entry) also carries `provider` \u2014 openai | anthropic | google, the vocabulary the weekday rotation in scripts/select-daily-model-target.mjs uses, plus `azure` for a model reached through an Azure AI Foundry deployment rather than through one of the rotated provider keys. `azure` is not a rotation value and never will be: the Foundry spec runs on every lane, outside the rotation. The field names WHO BILLED the tokens, which is what the QA Platform groups spend by \u2014 writing `openai` for a Foundry deployment would fold its spend into the same provider row as the real openai lanes, which are different accounts. It is written down rather than derived from the id prefix on purpose: the resolver here matches ids by substring in BOTH directions, so a prefix rule would put a second, different notion of identity on the same data. It has TWO consumers: scripts/sync-model-prices.mjs projects this file into the QA Platform's e2e_model_prices table, which carries provider as a column on each (price_key, since) row; and since #1300 `--summarize` reads it too, for the by_provider rollup on each token-history line (token-cost.mjs's resolveProvider). Note what this field can and cannot say, because the by_provider figure inherits it: it is keyed by model NAME, exactly like the rate, so the caveat on the gpt-5-mini row below — this row prices a NAME, and a different model deployed under that name is priced wrong with no gap to show it — applies to the provider just as it applies to the dollars. A genuine OpenAI call to an id this table declares `azure` is booked to azure.",
  "gpt-4o-mini": { "provider": "openai", "inputPerMillion": 0.15, "outputPerMillion": 0.6 },
  "gpt-4o": { "provider": "openai", "inputPerMillion": 2.5, "outputPerMillion": 10.0 },
  "gpt-5-mini": {
    "_comment": "Verified 2026-08-04 against OpenAI's published pricing ($0.25/$2.00 per MTok, developers.openai.com/api/docs/pricing) and cross-checked for Azure, which lists the same rates for this model — so the number is the same whichever account is billed. Needs its own row per the TIER rule above: Mini never inherits from a non-tier sibling, and it would not have matched anything anyway — the resolver requires one id to be a PREFIX of the other, and `gpt-5-mini` and `gpt-4o` diverge at the 4th character. Reported by run 30920300880 (2026-08-04, 584 tokens, 1 call) from tests-automations/regression/core-functionality/model-provider/azure-ai-foundry-provider-setup.spec.ts, where it left the whole run priced as a floor. CAVEAT WORTH RE-READING BEFORE TRUSTING THIS ROW: on Azure AI Foundry the model id in the trace is the PORTAL DEPLOYMENT NAME an operator typed (AZURE_AI_FOUNDRY_TEST_DEPLOYMENT), not a model identity the API vouches for. This row therefore prices a NAME. If the deployment is renamed, this row stops matching and the spend silently goes back to unpriced — visible as a price gap, which is the safe direction. If a DIFFERENT model is ever deployed under the name `gpt-5-mini`, this row keeps matching and prices it wrong with no gap to show it — the unsafe direction, and the reason this caveat is written down rather than assumed.",
    "provider": "azure",
    "inputPerMillion": 0.25,
    "outputPerMillion": 2.0
  },
  "claude-sonnet-4-6": { "provider": "anthropic", "inputPerMillion": 3.0, "outputPerMillion": 15.0 },
  "claude-sonnet-5": [
    {
      "_comment": "Introductory rate (#1211). `since` is the earliest date this table needs to answer for, not a verified launch date — only its ordering relative to the next band matters. Effective through 2026-08-31 per Anthropic's announced promotional window.",
      "since": "2026-01-01",
      "provider": "anthropic",
      "inputPerMillion": 2.0,
      "outputPerMillion": 10.0
    },
    {
      "_comment": "Standard list rate, effective 2026-09-01 once the introductory window ends.",
      "since": "2026-09-01",
      "provider": "anthropic",
      "inputPerMillion": 3.0,
      "outputPerMillion": 15.0
    }
  ],
  "claude-opus-5": { "provider": "anthropic", "inputPerMillion": 5.0, "outputPerMillion": 25.0 },
  "claude-haiku-4-5": {
    "_comment": "Verified 2026-08-03 against Anthropic's published pricing page ($1/$5 per MTok). Flat: no dated band is announced for this model, unlike claude-sonnet-5's introductory window. This is the model CANDIDATE_PREFS.anthropic leads with (collect-models.ts:148-155) — chosen for price — so it is the Anthropic id a real daily is most likely to report.",
    "provider": "anthropic",
    "inputPerMillion": 1,
    "outputPerMillion": 5
  },
  "claude-fable-5": {
    "_comment": "The other nine Anthropic ids the catalog exposes were priced together for #1255 item 4, from the published-rate table recorded in docs/superpowers/specs/2026-08-03-e2e-token-attribution-coverage-design.md §7.2.1 (sourced 2026-08-03 against platform.claude.com/docs/en/pricing) — re-verify before trusting a number more than a few months old. Why they are worth pricing rather than leaving to `unpriced_models`: rankCandidates falls through to raw catalog order when no /haiku/ and no /sonnet/ id is available (collect-models.ts:157-161), and this model at $10/$50 is the dearest thing behind that door — 10x haiku, 2x opus-5. Left unpriced, a run that fell through would report its most expensive day as a FLOOR with `n/a` on the largest row. `unpriced_models` remains the guard for an id nobody has priced yet; it should not be the guard for ids whose rates are published and known.",
    "provider": "anthropic",
    "inputPerMillion": 10.0,
    "outputPerMillion": 50.0
  },
  "claude-opus-4-8": { "provider": "anthropic", "inputPerMillion": 5.0, "outputPerMillion": 25.0 },
  "claude-opus-4-7": { "provider": "anthropic", "inputPerMillion": 5.0, "outputPerMillion": 25.0 },
  "claude-opus-4-6": { "provider": "anthropic", "inputPerMillion": 5.0, "outputPerMillion": 25.0 },
  "claude-opus-4-5": { "provider": "anthropic", "inputPerMillion": 5.0, "outputPerMillion": 25.0 },
  "claude-opus-4-1": {
    "_comment": "Deprecated upstream, still listed in the collected catalog (tests/helpers/provider-setup/data/models.json), so still reachable through the raw-order fall-through. $15/$75 per §7.2.1.",
    "provider": "anthropic",
    "inputPerMillion": 15.0,
    "outputPerMillion": 75.0
  },
  "claude-opus-4-20250514": { "provider": "anthropic", "inputPerMillion": 15.0, "outputPerMillion": 75.0 },
  "claude-sonnet-4-5": { "provider": "anthropic", "inputPerMillion": 3.0, "outputPerMillion": 15.0 },
  "claude-sonnet-4-20250514": { "provider": "anthropic", "inputPerMillion": 3.0, "outputPerMillion": 15.0 },
  "gemini-2.5-flash": { "provider": "google", "inputPerMillion": 0.3, "outputPerMillion": 2.5 },
  "gemini-flash-latest": { "provider": "google", "inputPerMillion": 0.3, "outputPerMillion": 2.5 },
  "gemini-3.5-flash": { "provider": "google", "inputPerMillion": 0.3, "outputPerMillion": 2.5 },
  "gemini-2.5-flash-lite": { "provider": "google", "inputPerMillion": 0.1, "outputPerMillion": 0.4 },
  "gemini-3.5-flash-lite": { "provider": "google", "inputPerMillion": 0.1, "outputPerMillion": 0.4 },
  "gemini-flash-lite-latest": { "provider": "google", "inputPerMillion": 0.1, "outputPerMillion": 0.4 }
}
