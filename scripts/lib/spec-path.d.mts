// Types for spec-path.mjs — see scripts/lib/tmp-dir.d.mts for why the `.mjs`
// helpers carry a hand-written declaration rather than being compiled.
export declare function normalizeSpecPath(file: string): string;
